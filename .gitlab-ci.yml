image: python:3.7.2-stretch

stages:
  - build

prepare:
  stage: build
  script:
  - pip install urllib3
  - pip install requests
  - python asm.py
  artifacts:
    paths:
    - public/
  only:
  - master
